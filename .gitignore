node_modules/
.next/
out/
build/

# Локальная база стенда и загруженные картинки.
# Слэш в начале обязателен: без него шаблон срабатывает на любом уровне
# и заодно исключает src/data — банк вопросов, без которого квиз не работает.
/data/
public/uploads/*
!public/uploads/.gitkeep

.env
.env.local
.env*.local

*.log
npm-debug.log*
.DS_Store
Thumbs.db
next-env.d.ts
tsconfig.tsbuildinfo
.claude/

# Локальный Postgres для разработки
.pgdata/
