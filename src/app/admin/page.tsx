import { AdminApp } from '@/components/admin/AdminApp';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Teboil — панель оператора',
};

export default function AdminPage() {
  return <AdminApp />;
}
